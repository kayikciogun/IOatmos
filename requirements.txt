# Python 3.12 için tam bağımlılık listesi
# laion-clap 1.1.6'nın pyproject.toml'undan alındı, sadece numpy<2.0 pin'i bypass edildi.
# Kurulum:
#   pip install -r requirements.txt
#   pip install --no-deps laion-clap==1.1.6

# Core ML
torch>=2.4.0
torchaudio>=2.4.0
torchvision>=0.19.0
transformers>=4.51.3

# Audio
librosa>=0.10.0
soundfile>=0.12.1
torchlibrosa

# Numerik (numpy<2 — laion-clap eski kod kullanıyor)
numpy>=1.23.5,<2.0
scipy>=1.15.0
scikit-learn
pandas>=2.2.0
llvmlite

# Veri / yardımcı
h5py
tqdm
regex
ftfy
braceexpand
webdataset
wget
progressbar2
wandb